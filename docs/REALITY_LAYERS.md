# The Reality Layers — design groundwork

CATSINO is not a casino with lore bolted on; it is the **Hyperliminal layer**
of a six-layer cosmology. This doc is the canonical spec; the code under
`godot/src/layers/` is its implementation groundwork.

## The six layers

| Layer | What it is | Entry | PvP | Persistence | Currency |
|---|---|---|---|---|---|
| **Hyperliminal** | Catsino itself — the neon casino reality (districts, games, tournaments) | always | no | static | 🪙 Cat Coins |
| **Liminal** | The between-space. Multiplayer, procedural, **never static** — chunks dissolve behind you | always | yes | ephemeral | 🌫️ Essence |
| **Subliminal** | Invite-only start screen + one small apartment per player where ALL UGC building happens. 3 outstanding invites max; creator subscription raises the cap | invite | no | personal | ✨ Spark* |
| **Supraliminal** | The main MMORPG — the DFW Metroplex | always | outside hubs | chunked, infinite | 🏴 Influence |
| **Extraliminal** | Pokemon-GO-style real-world overlay — roaming roster entities, guild halls at claimable landmarks | always | guild wars | landmark | 🔮 Sigils |
| **Periliminal** | The psychological layer. You can't enter it — it takes you after wandering the Liminal too long | pulled only | no | generated-then-static | 👁️ Whispers |

*Spark is reserved in `RealityLayers` but not yet in `EconomyManager.CURRENCIES`
(currently: cat_coins, gems, essence, influence, sigil, whisper — gems being
the premium cross-layer currency). If Spark should replace gems as currency
#6 or be #7, decide before the economy ships.

## Supraliminal: DFW Metroplex

- **Arlington** — neutral PvE center, the factionless starting city. Contains
  the four inner hubs: **Marketplace**, **Arena** (all minigame modes — see
  below), **Workshop/University**, and the **Space Station** (gate to other
  worlds).
- **Dallas** (SovereignCrown), **Fort Worth** (VeiledCurrent), **Denton**
  (WildlandsAscendant) — the three faction hubs, laid out to real geography
  on the chunk grid (`hub_region_data.gd`).
- Hub interiors: always PvE. **Everything outside hub bounds is PvP** and
  claimable (`territory_control.gd`), procedurally generated by the existing
  chunk system (`DiscoveryManager` + `ProceduralRegionGenerator`) and scales
  infinitely.
- **Sovereign of the Metroplex** (ESO-emperor-like, tweaked): alliances earn
  weighted territory (keystone chunks ringing hub exits are worth 5×). When
  the leading alliance crosses the threshold, its **top contributor** is
  crowned — and the crown buffs the *whole alliance* (+15%), not just the
  wearer. Lose the territory lead, lose the crown.

## Arena modes (not separate layers)

MOBA ("Paws of the Ancients"), Faction Conflict, survival ("Last Cat
Standing"), zombies ("Feral Horde"), CTF ("Yarn Rush"), stadium racing —
all **modes of the Arena hub** (`arena_modes.gd`), sharing matchmaking and
economy. Recommendation: keep them as modes; promote one to a full layer
only if it grows persistent worlds/economy of its own.

## Periliminal rules (`periliminal_runs.gd`)

- Pulled in after **10 minutes** of Liminal wandering (2-minute warning).
- Chunks generate on first entry, then the seed is ledgered — **static
  forever**, same space for every future visitor.
- **Death loses everything**: entities, inventory, every currency except
  already-banked Whispers. **Group shared fate: one dies, all die.**
- Rewards compound with depth (3, 9, 18, 30, 45…) and Whispers are earned
  *only* here — they buy the rarest blueprints, one entity resurrection, and
  permanent account perks.

## Extraliminal (`extraliminal_manager.gd`)

- Roaming entities rolled rarity-weighted from the ~600-entity roster.
- Landmarks are claimable **guild halls**. A guild war starts when one
  challenger **opens a liminal door** at the landmark (Secret-Power-style
  hidden entrance, costs Essence) and their guild pours through to fight.

## Identity & perception

- **20 races × 20 frames × 20 mods** (`race_data_character.gd`,
  `frame_mod_data.gd`) feed stats, the procedural rig, and influence packs.
- **Every roster entity is also a UGC blueprint** (`entity_blueprint.gd`):
  fork it in your Subliminal apartment, remix within a locked stat budget,
  submit through the Discord review pipeline.
- **Asymmetric perception** (`perception_system.gd`): how you render on
  someone else's client depends on relative strength, faction type advantage
  (SovereignCrown > WildlandsAscendant > VeiledCurrent > SovereignCrown),
  and alignment. Stronger players loom; countered players look diminished;
  much weaker viewers can't even read a strong player's loadout.

## Wiring status

Implemented groundwork: layer registry + manager, wander-pull, territory/
Sovereign system, DFW hub layout, six-currency economy with sources/sinks,
subliminal invites + apartment slots + creator sub, periliminal run manager
with wipe rules, extraliminal spawns/catches/guild wars, perception contract,
entity blueprints, arena mode registry, layer-select UI, and shared 3D layer
scenes reusing the overworld kit. Not yet wired: multiplayer replication,
GPS/real-map landmarks, per-mode arena gameplay, apartment interior scene,
perception hooks inside CharacterRig rendering.
